"use client";import{useEffect,useMemo,useState}from"react";import AppShell from"@/components/app-shell";export default function Page(){const[d,setD]=useState<any>(null),[cap,setCap]=useState(20),[ready,setReady]=useState(true);useEffect(()=>{fetch('/api/executive-demo').then(r=>r.json()).then(j=>j.success&&setD(j))},[]);const s=useMemo(()=>{if(!d)return null;const baseLate=d.risks.filter((x:any)=>x.risk==='LATE/BLOCKED').length,baseAt=d.risks.filter((x:any)=>x.risk==='AT RISK').length,baseGap=d.kpi.setGapQty,baseOver=d.kpi.overloadedWorkCenters;const gain=Math.max(0,Math.round((cap/100)*d.recommendations.reduce((a:number,x:any)=>a+Number(x.recommendedQty||0),0)));return{baseLate,baseAt,baseGap,baseOver,newLate:Math.max(0,baseLate-Math.floor(gain/25)-(ready?1:0)),newAt:Math.max(0,baseAt-Math.floor(gain/40)),newGap:Math.max(0,baseGap-gain),newOver:Math.max(0,baseOver-(cap>=15?1:0))}},[d,cap,ready]);if(!s)return <AppShell><main className="p-5">Đang tải...</main></AppShell>;return <AppShell><main className="mx-auto max-w-[1400px] p-5"><p className="text-xs font-black uppercase text-violet-600">Scenario Compare</p><h1 className="text-2xl font-black">So sánh phương án kế hoạch A / B</h1><p className="text-sm text-slate-500">Mô phỏng demo, không ghi dữ liệu thật.</p><div className="mt-5 grid gap-3 md:grid-cols-2"><div className="rounded-xl border bg-white p-4"><b>A • Hiện trạng</b><p className="mt-2 text-sm text-slate-500">Capacity và Material hiện tại.</p></div><div className="rounded-xl border border-violet-200 bg-violet-50 p-4"><b>B • Phương án cải thiện</b><div className="mt-3">Capacity +{cap}%</div><input type="range" min="0" max="50" value={cap} onChange={e=>setCap(+e.target.value)} className="w-full"/><label className="mt-3 flex gap-2 text-sm"><input type="checkbox" checked={ready} onChange={e=>setReady(e.target.checked)}/> Giả định xử lý Material shortage ưu tiên</label></div></div><div className="mt-5 overflow-hidden rounded-xl border bg-white"><table className="w-full text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">KPI</th><th>A - Hiện trạng</th><th>B - Mô phỏng</th><th>Thay đổi</th></tr></thead><tbody>{[["Late/Blocked",s.baseLate,s.newLate],["At Risk",s.baseAt,s.newAt],["Set Gap",s.baseGap,s.newGap],["WO Overload",s.baseOver,s.newOver]].map(x=><tr className="border-t" key={String(x[0])}><td className="p-3 font-bold">{x[0]}</td><td className="text-center">{x[1]}</td><td className="text-center font-black text-violet-700">{x[2]}</td><td className="text-center">{Number(x[2])-Number(x[1])}</td></tr>)}</tbody></table></div></main></AppShell>}
